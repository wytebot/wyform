import React,{useState} from 'react';import {signInWithPopup,auth,googleProvider} from '../lib/firebase';import {Code2,Mail,Table2,ShieldCheck} from 'lucide-react';
export default function Auth(){const [busy,setBusy]=useState(false),[error,setError]=useState('');async function login(){setBusy(true);setError('');try{await signInWithPopup(auth,googleProvider)}catch(e){setError(e.message||'Could not sign in.')}finally{setBusy(false)}}return <div className="auth"><div className="authCard"><div className="brand big"><span className="logoMark">F</span> WyForm</div><div className="eyebrow">THE 10-SECOND BACKEND FOR ANY HTML FORM</div><h1>Forms without PHP, Node or servers.</h1><p className="muted">Add one script and one attribute. WyForm receives submissions, sends email from your Gmail, and can sync every lead to Google Sheets.</p><div className="pills"><span><Code2 size={15}/>1 script</span><span><Mail size={15}/>Your Gmail</span><span><Table2 size={15}/>Google Sheets</span><span><ShieldCheck size={15}/>Spam shield</span></div><button className="primary wide" disabled={busy} onClick={login}>{busy?'Opening Google…':'Continue with Google'}</button>{error&&<div className="error">{error}</div>}<p className="tiny">By continuing you agree to the <a href="/terms">Terms</a> and <a href="/privacy">Privacy Policy</a>.</p></div></div>}
