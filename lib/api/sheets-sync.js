import {googleAccessToken} from './google-token.js';
import {getUser,json,method} from './_supabase.js';
export default async function handler(req,res){if(!method(req,res,['POST']))return;try{const u=await getUser(req);const b=req.body||{};const token=await googleAccessToken(u.uid);const spreadsheetId=String(b.spreadsheetId||'');const range=String(b.range||'Sheet1!A:Z');const values=[Array.isArray(b.values)?b.values:[]];const r=await fetch(`https://sheets.googleapis.com/v4/spreadsheets/${encodeURIComponent(spreadsheetId)}/values/${encodeURIComponent(range)}:append?valueInputOption=USER_ENTERED&insertDataOption=INSERT_ROWS`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({values})});const d=await r.json();if(!r.ok)throw new Error(d.error?.message||'Sheets append failed.');return json(res,200,{ok:true,result:d});}catch(e){return json(res,500,{error:e.message||'Sheets sync failed.'})}}
